# create a vector with 'start:finish' notation
w <- 18:23

# create a vector with c() function which combines all arguments to form a vector
h <- c(1.3, 2.8, 7.0, 0.9, 3.1, 0.2)

# combine together to build a data frame
data <- data.frame(width=w, height=h)
