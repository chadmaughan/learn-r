# where is the working directory?
getwd()

# you can also set the working directory
#  setting this manually doesn't
setwd("/tmp")

getwd()

# to have the editor always startup in your working directory
# go to preferences -> Startup, change 'Initial working directory' and check "Always apply"
